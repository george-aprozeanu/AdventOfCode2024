#include "Day1.h"
#include "Day2.h"
#include <algorithm>
#include <iostream>

int main(int argc, char* argv[])
{
	auto infile = arg_infile(argc, argv);
	//day1p1(infile);
	//day1p2(infile);
	// day2p1(infile);
	day2p2(infile);
}
